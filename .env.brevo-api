# BREVO API CONFIGURATION (Instead of SMTP)
# This works on ANY hosting platform - no port blocking issues!

# Get your API key from: https://app.brevo.com/settings/keys/api

BREVO_API_KEY=your_brevo_api_key_here

# Email settings for API
MAIL_MAILER=brevo_api
MAIL_FROM_ADDRESS=ustpbalubal.dmcrs@gmail.com
MAIL_FROM_NAME="USTP Balubal Campus - DMCRS"

# Database settings (keep your current setup)
DB_CONNECTION=pgsql
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_DATABASE=${PGDATABASE}
DB_USERNAME=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

# Instructions:
# 1. Go to https://app.brevo.com/settings/keys/api
# 2. Create new API key
# 3. Replace "your_brevo_api_key_here" with actual key
# 4. Update Render environment variables
# 5. Redeploy

# Benefits:
# ✅ Works on all hosting platforms
# ✅ No SMTP port issues  
# ✅ Faster email delivery
# ✅ Better error handling
# ✅ Same 300 emails/day limit