# MAILGUN BACKUP CONFIGURATION (if Brevo keeps timing out)
# Sign up at https://mailgun.com (FREE 100 emails/day)

# Mailgun SMTP Settings
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_USERNAME=postmaster@your-mailgun-domain.mailgun.org
MAIL_PASSWORD=your_mailgun_password_here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=ustpbalubal.dmcrs@gmail.com
MAIL_FROM_NAME="USTP Balubal Campus - DMCRS"

# Mailgun Connection Settings
MAIL_TIMEOUT=120
MAIL_CONNECTION_TIMEOUT=60
MAIL_STREAM_TIMEOUT=90

# Instructions:
# 1. Go to https://mailgun.com
# 2. Create free account (100 emails/day)  
# 3. Get SMTP credentials from dashboard
# 4. Update Render environment variables if Brevo fails